Whenever a new requirement is added to the application, be sure to make sure it is reflected in SPEC.

For all spatial tile layers, support over-zoom by showing the highest-resolution native tiles on higher zoom levels (for example by pairing `maxNativeZoom` with a higher `maxZoom`).

Always run a javascript linter and look for issues before completing a javascript task.
